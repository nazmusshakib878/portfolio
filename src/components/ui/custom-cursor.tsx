'use client'
import { useEffect,useRef } from 'react'
export function CustomCursor(){const dot=useRef<HTMLDivElement>(null);const ring=useRef<HTMLDivElement>(null);useEffect(()=>{if(matchMedia('(pointer: coarse)').matches||matchMedia('(prefers-reduced-motion: reduce)').matches)return;let x=-30,y=-30,rx=-30,ry=-30,raf=0;const move=(event:PointerEvent)=>{x=event.clientX;y=event.clientY;dot.current?.style.setProperty('transform',`translate3d(${x}px,${y}px,0)`)};const tick=()=>{rx+=(x-rx)*.16;ry+=(y-ry)*.16;ring.current?.style.setProperty('transform',`translate3d(${rx}px,${ry}px,0)`);raf=requestAnimationFrame(tick)};addEventListener('pointermove',move,{passive:true});raf=requestAnimationFrame(tick);return()=>{removeEventListener('pointermove',move);cancelAnimationFrame(raf)}},[]);return <><div ref={dot} className="pointer-events-none fixed left-0 top-0 z-[90] hidden size-1.5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-white mix-blend-difference lg:block"/><div ref={ring} className="pointer-events-none fixed left-0 top-0 z-[89] hidden size-8 -translate-x-1/2 -translate-y-1/2 rounded-full border border-white/40 lg:block"/></>}
