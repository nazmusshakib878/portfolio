# Personal Portfolio

A simple, single-page personal portfolio website.

## Run locally

You can open `/home/runner/work/portfolio/portfolio/index.html` directly in a browser, or serve the directory:

```bash
cd /home/runner/work/portfolio/portfolio
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
